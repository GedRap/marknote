<div class="row">
	<div class="col-md-12">
		<h3>Notes under <%= @tag.name %></h3>
	</div>
	<div class="col-md-12">
		<% @notes.each do |note| %>
			<p><a href="/notes/<%= note.id %>"><%= note.title %></a></p>
		<% end %>
	</div>
</div>