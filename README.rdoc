== MarkNote 

A lightweight application to store and organize personal notes.

Automatically converts from MarkDown to HTML (thus the name).

The project is in a very early stage, going to create a web site and some docs once ready.
